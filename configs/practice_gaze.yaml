# Live MindLink practice override for the authoritative gaze-interaction defaults.
# Provisional camera-rate value: pilot-tune against the effective processed-scene
# rate after detector latency and queue drops are measured on the target computer.
tracker:
  frame_rate: 30

visualization:
  save_frames: false
  show_window: false
